# Dev log — 2026-09-08

Fork session on the laptop checkout (`feat/roguelite-weapons`). Everything below
landed in commit **`6c964517b`** ("feat: character HUD, window scaling,
plasma-heat power-ups, roster changes") unless noted.

## Environment

- Cloned the fork to `C:\Users\bvlsc\source\crimsonland` (the original scratch
  path blew past Windows' path limit).
- Installed `uv` 0.12.10, first `uv run crimson --test-mode --no-intro` built the
  venv, pulled the base-game PAQ archives, ran green.
- Focused test suite (`tests/weapons tests/sim tests/bonuses tests/render
  tests/progression tests/projectiles`): all pass. The `test_zig_*` /
  `test_match` / `test_mod_sdk` / `test_library_*` failures are the documented
  pre-existing set (no capstone / zig toolchain here).

## Window scaling

Problem: the raylib window was fixed at 1024×768 and the maximise button was
disabled; menus were laid out for 1024 and anchored top-left; on a 16:9 monitor
there were black bars.

- New `src/grim/letterbox.py` — the frame renders to an off-screen target at a
  **virtual resolution that tracks the window's aspect ratio** (locked on the
  shorter axis to the configured base size) and blits to fill the whole window.
  No bars, no distortion, resizable / maximise / fullscreen all work.
- `rl.get_screen_*` / `rl.get_render_*` / `rl.get_mouse_*` are patched to virtual
  space so the world renderer, HUD, menus and mouse aim all follow.
- Every patched accessor derives its result **live from the current window
  size** each call — fixes an intermittent "mouse snaps to the bottom-left
  quadrant" caused by (a) a stale per-frame scale factor after a resize and
  (b) clamping the mapped cursor to a window corner when it left the window.
- `src/grim/app.py` `run_view(virtual_size=, on_virtual_resize=)`;
  `src/grim/terrain_render.py` defers ground baking while the letterbox target
  is open (avoids nested `begin_texture_mode`).
- `src/crimson/game/runtime.py` keeps `cfg.display` in step with the virtual
  size and keeps `FLAG_WINDOW_RESIZABLE`.
- Tests: `tests/grim/test_letterbox.py`.

## Character-anchored HUD

`src/crimson/render/world/player_status.py`, flags in `src/crimson/ui/hud.py`:

- **Health** → a depleting red ring around the player (full circle at 100%,
  drains clockwise from the top, pulses ≤30%).
- **Clip ammo** → a number at the player's upper-right.
- **Power-ups** → a FIFO icon stack at the player's upper-left, oldest (front of
  the queue) nearest the character, newer ones stacking upward, each with its
  remaining whole seconds in the icon's top-left corner. Non-rotating.
- `HUD_VITALS_ON_CHARACTER` suppresses the top-bar heart / health bar / ammo
  pips; `HUD_POWERUP_ON_CHARACTER` suppresses the sliding bonus panel. Weapon
  icon, clock, XP panel and score stay.
- `src/crimson/render/world/bonus_icons.py` — Fork Shot and Blade draw their own
  icon (Fork Shot procedurally, Blade from the orbiting-blade sprite) both on
  the **ground pickup** and in the HUD stack, since the shared bonus sheet has
  no usable frame for either. Drawn upright (no wobble / spin).
- Tests: `tests/render/test_player_powerup_stack.py`.

## Plasma clip-heat + power-ups

`src/crimson/weapon_runtime/plasma_heat.py`, `power_up.py`,
`render/projectile_draw/primary_plasma.py`:

- **Weapon Power Up** no longer gives plasma a fire-rate bonus (all six plasma
  weapons added to `WPU_NO_RATE_WEAPON_IDS`). Instead it raises the clip-heat
  **floor**: energy damage starts at **+H (+50%)** on a fresh clip and ramps to
  **+2H (+100%)** by the stock clip's last round (keeps climbing past +2H with a
  wider clip).
- **Reflex Boost** pins plasma clip-heat to a **flat +H** for its whole
  duration (ammo isn't spent under Reflex Boost, so there's no clip to ramp
  against). Reflex Boost wins if both power-ups are active.
- Bolt / aura colour now lerps from the **base colour at 0 heat → white-hot at
  +H** (`heat_t = heat_fraction / PLASMA_HEAT_H`). Was a partial shift toward
  warm orange, maxing only at +2H.
- Tests: `tests/weapons/test_plasma_heat.py`, `test_weapon_power_up_classes.py`.

## Bonuses

- **Energizer removed** — always suppressed in `bonuses/selection.py` (the
  native RNG roll still runs, so pick determinism is preserved; it just rerolls
  to another bonus), no apply handler, `bonuses/energizer.py` deleted. The inert
  flee/eat mechanic + timer field are left in place (removing them touches
  parity-critical creature runtime / dbg / replay for no functional gain).
- **Fork Shot / Blade** folded into the normal drop pool in **every mode** —
  `fork_bonus_in_pool` now defaults on (was Maps-only).
- Tests: `tests/bonuses/test_fork_bonus_in_pool.py`,
  `test_bonus_pick_random_type_quest_suppression.py`,
  `test_test_mode_fork_spawner.py`, `tests/modes/test_gameplay_modes_smoke.py`.

## Weapons

- **Evil Scythe** (id 27) and **Arc Gun** (id 33) added to the always-available
  main roster (`_FORK_ROSTER_WEAPON_IDS` in `weapon_runtime/availability.py`) —
  they now drop from the normal Weapon bonus in every mode.
- **Debug weapon cycle (`[` / `]`)** and the **`--test-mode` unlock-all** are now
  restricted to `fireable_weapon_ids()` (new, in `weapon_runtime/fire_recipes.py`).
  Fixes a crash when `[` cycled onto a cut / unimplemented stub — Nuke Launcher,
  Flameburst, Grim Weapon, Transmutator, Blaster R-300, Lightning Rifle — none of
  which have a projectile template (`ValueError: weapon has no primary
  projectile type`).
- Tests: `tests/weapons/test_fireable_weapon_ids.py`.

### Explored and reverted

- Briefly wired the cut **Lightning Rifle** (id 52) as a working chain-lightning
  rifle (shared the Arc Gun runtime), then removed it on request. Fully reverted.

## Test / debug mode

- `--test-mode`: `_TEST_MODE_BONUS_CYCLE` and `_TEST_MODE_WEAPON_DROPS` are empty
  again — no forced bonus rain, no free weapon at spawn. It now unlocks the
  fireable weapon set so every weapon can turn up on a Weapon bonus.
- `SETUP.md` §5 (test/debug) and §6 (mod content table) updated to match.

## Test status

Full run (excluding the documented pre-existing `zig` / `match` / `mod_sdk` /
`library` set): **2407 passed, 16 skipped**. Pre-existing `ruff check` import-sort
debt in unrelated files was left as-is; the new code adds none.
