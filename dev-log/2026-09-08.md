# Dev log — 2026-09-08

Fork session on the laptop checkout (`feat/roguelite-weapons`). Everything below
landed in commit **`6c964517b`** ("feat: character HUD, window scaling,
plasma-heat power-ups, roster changes") unless noted.

## Environment

- Cloned the fork to `C:\Users\bvlsc\source\crimsonland` (the original scratch
  path blew past Windows' path limit).
- Installed `uv` 0.12.10, first `uv run crimson --test-mode --no-intro` built the
  venv, pulled the base-game PAQ archives, ran green.
- Focused test suite (`tests/weapons tests/sim tests/bonuses tests/render
  tests/progression tests/projectiles`): all pass. The `test_zig_*` /
  `test_match` / `test_mod_sdk` / `test_library_*` failures are the documented
  pre-existing set (no capstone / zig toolchain here).

## Window scaling

Problem: the raylib window was fixed at 1024×768 and the maximise button was
disabled; menus were laid out for 1024 and anchored top-left; on a 16:9 monitor
there were black bars.

- New `src/grim/letterbox.py` — the frame renders to an off-screen target at a
  **virtual resolution that tracks the window's aspect ratio** (locked on the
  shorter axis to the configured base size) and blits to fill the whole window.
  No bars, no distortion, resizable / maximise / fullscreen all work.
- `rl.get_screen_*` / `rl.get_render_*` / `rl.get_mouse_*` are patched to virtual
  space so the world renderer, HUD, menus and mouse aim all follow.
- Every patched accessor derives its result **live from the current window
  size** each call — fixes an intermittent "mouse snaps to the bottom-left
  quadrant" caused by (a) a stale per-frame scale factor after a resize and
  (b) clamping the mapped cursor to a window corner when it left the window.
- `src/grim/app.py` `run_view(virtual_size=, on_virtual_resize=)`;
  `src/grim/terrain_render.py` defers ground baking while the letterbox target
  is open (avoids nested `begin_texture_mode`).
- `src/crimson/game/runtime.py` keeps `cfg.display` in step with the virtual
  size and keeps `FLAG_WINDOW_RESIZABLE`.
- Tests: `tests/grim/test_letterbox.py`.

## Character-anchored HUD

`src/crimson/render/world/player_status.py`, flags in `src/crimson/ui/hud.py`:

- **Health** → a depleting red ring around the player (full circle at 100%,
  drains clockwise from the top, pulses ≤30%).
- **Clip ammo** → a number at the player's upper-right.
- **Power-ups** → a FIFO icon stack at the player's upper-left, oldest (front of
  the queue) nearest the character, newer ones stacking upward, each with its
  remaining whole seconds in the icon's top-left corner. Non-rotating.
- `HUD_VITALS_ON_CHARACTER` suppresses the top-bar heart / health bar / ammo
  pips; `HUD_POWERUP_ON_CHARACTER` suppresses the sliding bonus panel. Weapon
  icon, clock, XP panel and score stay.
- `src/crimson/render/world/bonus_icons.py` — Fork Shot and Blade draw their own
  icon (Fork Shot procedurally, Blade from the orbiting-blade sprite) both on
  the **ground pickup** and in the HUD stack, since the shared bonus sheet has
  no usable frame for either. Drawn upright (no wobble / spin).
- Tests: `tests/render/test_player_powerup_stack.py`.

## Plasma clip-heat + power-ups

`src/crimson/weapon_runtime/plasma_heat.py`, `power_up.py`,
`render/projectile_draw/primary_plasma.py`:

- **Weapon Power Up** no longer gives plasma a fire-rate bonus (all six plasma
  weapons added to `WPU_NO_RATE_WEAPON_IDS`). Instead it raises the clip-heat
  **floor**: energy damage starts at **+H (+50%)** on a fresh clip and ramps to
  **+2H (+100%)** by the stock clip's last round (keeps climbing past +2H with a
  wider clip).
- **Reflex Boost** pins plasma clip-heat to a **flat +H** for its whole
  duration (ammo isn't spent under Reflex Boost, so there's no clip to ramp
  against). Reflex Boost wins if both power-ups are active.
- Bolt / aura colour now lerps from the **base colour at 0 heat → white-hot at
  +H** (`heat_t = heat_fraction / PLASMA_HEAT_H`). Was a partial shift toward
  warm orange, maxing only at +2H.
- Tests: `tests/weapons/test_plasma_heat.py`, `test_weapon_power_up_classes.py`.

## Bonuses

- **Energizer removed** — always suppressed in `bonuses/selection.py` (the
  native RNG roll still runs, so pick determinism is preserved; it just rerolls
  to another bonus), no apply handler, `bonuses/energizer.py` deleted. The inert
  flee/eat mechanic + timer field are left in place (removing them touches
  parity-critical creature runtime / dbg / replay for no functional gain).
- **Fork Shot / Blade** folded into the normal drop pool in **every mode** —
  `fork_bonus_in_pool` now defaults on (was Maps-only).
- Tests: `tests/bonuses/test_fork_bonus_in_pool.py`,
  `test_bonus_pick_random_type_quest_suppression.py`,
  `test_test_mode_fork_spawner.py`, `tests/modes/test_gameplay_modes_smoke.py`.

## Weapons

- **Evil Scythe** (id 27) and **Arc Gun** (id 33) added to the always-available
  main roster (`_FORK_ROSTER_WEAPON_IDS` in `weapon_runtime/availability.py`) —
  they now drop from the normal Weapon bonus in every mode.
- **Debug weapon cycle (`[` / `]`)** and the **`--test-mode` unlock-all** are now
  restricted to `fireable_weapon_ids()` (new, in `weapon_runtime/fire_recipes.py`).
  Fixes a crash when `[` cycled onto a cut / unimplemented stub — Nuke Launcher,
  Flameburst, Grim Weapon, Transmutator, Blaster R-300, Lightning Rifle — none of
  which have a projectile template (`ValueError: weapon has no primary
  projectile type`).
- Tests: `tests/weapons/test_fireable_weapon_ids.py`.

### Explored and reverted

- Briefly wired the cut **Lightning Rifle** (id 52) as a working chain-lightning
  rifle (shared the Arc Gun runtime), then removed it on request. Fully reverted.

## Test / debug mode

- `--test-mode`: `_TEST_MODE_BONUS_CYCLE` and `_TEST_MODE_WEAPON_DROPS` are empty
  again — no forced bonus rain, no free weapon at spawn. It now unlocks the
  fireable weapon set so every weapon can turn up on a Weapon bonus.
- `SETUP.md` §5 (test/debug) and §6 (mod content table) updated to match.

## Test status

Full run (excluding the documented pre-existing `zig` / `match` / `mod_sdk` /
`library` set): **2407 passed, 16 skipped**. Pre-existing `ruff check` import-sort
debt in unrelated files was left as-is; the new code adds none.

---

# Dev log — 2026-09-08 (follow-up session)

Continued on `feat/roguelite-weapons`, on top of `6c964517b` / `6a3c3ac0f`.

## Landed

### Score-screen crash (`ReplayCheckpointsError`)
`_record_replay_checkpoint` (`modes/base_gameplay_mode.py`) only skipped an exact
duplicate of the last recorded tick. The `X` debug XP-grant / level-up spam pokes
player state off-tick and fed the recorder an out-of-order tick, which the
checkpoint format validator hard-rejects at the game-over save
(`checkpoint tick indices must be strictly increasing and unique`). Now it drops
any checkpoint whose tick is `<=` the last one. Verified: died after spamming
`X` + perks, score screen renders and the replay saves.

### Top bar removed / weapon icon relocated (`ui/hud.py`)
- `HUD_TOP_BAR = False` — the `ui_game_top` background strip is no longer drawn.
- `HUD_WEAPON_TL_*` — for single-player with the bar off, the weapon icon parks
  in the top-left corner on its own black backing box, full opacity.

### Player HUD font bump (`grim/fonts/small.py`, `render/world/player_status.py`)
- `draw_small_text` / `measure_small_text_width` gained an optional `scale`.
- The on-player clip ammo and power-up seconds render at `_HUD_TEXT_SCALE = 1.2`
  (one notch over the 16px default; the earlier view-scaled multiplier was too
  big and was reverted).

### `cfg.display` drift fixed (`game/runtime.py`)
Root cause of the ballooning virtual resolution: `_on_virtual_resize` writes the
runtime virtual size into `cfg.display.width/height`, and `config.save()` (called
on death and menu transitions) persists it - so every session read its own last
window-aspect size back as the *base*, and it compounded (found `crimson.cfg` at
`1679x898`). Fix:
- The letterbox base is now a fixed `1024x768`, never read from `cfg.display`.
- OS window size comes from `--width/--height` or the saved pref, sanitised to a
  1280x960 fallback if the cfg value is out of a sane range.
- Reset the corrupted local `artifacts/runtime/crimson.cfg` (not tracked).

### DPI awareness (`grim/app.py`)
`run_view` now calls `SetProcessDpiAwarenessContext(PER_MONITOR_AWARE_V2)` (with
`SetProcessDpiAwareness` / `SetProcessDPIAware` fallbacks) before `init_window`.
On this machine (3840x2160 display, Windows display scaling) a DPI-unaware
process was getting its window virtualised: raylib rendered at the requested
size and Windows bitmap-scaled the output to the screen (blurry), and
`GetMousePosition` / `GetScreenWidth` ended up in different unit spaces.

### Blade power-up debug view (`render/world/blades.py`)
Under `--debug`: a cyan circle for the orbit path (`BLADE_RADIUS`), a red circle
for each blade's contact hitbox (`BLADE_HIT_RADIUS`, before the per-creature
size term), and a yellow centre dot per blade.

### Debug overlay readout (`modes/survival_mode.py`)
Under `--debug`, a new line: `mouse virt=(x,y) raw=(x,y) vsize=WxH  weapon=<name>
ammo=n/clip` - so mouse drift and weapon-pickup behaviour can be read off-screen.
`grim/letterbox.raw_mouse_position()` exposes the unpatched cursor.

## Open / needs more work

### Mouse still drifts to the lower-left
Even after the earlier "don't clamp + recompute live" fix, the reticle
occasionally jumps. With the new debug readout the user saw it land at virtual
**`(187, 457)`** once, and another point near it. Very likely the DPI unit-space
mismatch described above (raw cursor in one space, window size in another,
letterbox divides one by the other -> cursor confined to a fraction of the
screen). The `SetProcessDpiAwarenessContext` change should address it; needs
confirmation on the user's machine, and if it persists, `_mouse_virtual` in
`grim/letterbox.py` should map from `get_render_width/height` (physical
framebuffer) rather than `get_screen_width/height`.

### "Music / sound screwed up"
Reported after several sessions. A fresh launch shows a clean WASAPI/miniaudio
init (18 streams, no errors), and the debug FPS logging showed a solid 60 fps
even at the (then still ballooned) virtual resolution, so it is not an obvious
frame-time / starvation issue. Possibly downstream of the DPI double-upscale on
the integrated GPU, or the ballooned virtual res, or environmental. Awaiting a
description of *how* it sounds (stutter / distortion / pitch / silence).

### Splitter Gun picked up but weapon didn't change
User reported the Splitter Gun (id 29) ground pickup not swapping their weapon.
`apply_weapon` -> `weapon_assign_player` has no guard, so it should equip;
`MY_FAVOURITE_WEAPON` (perk 48, from the `F3` spam) blocks weapon *pickups* and
is the likely explanation, but the new debug `weapon=` readout will confirm
whether the id actually changes on pickup.

## Test status
Non-preexisting suite green (872 passed in the modes/render/ui/grim/replay/
weapons/bonuses/sim sweep). Pre-existing `ruff` import-sort debt untouched.


---

# Dev log — 2026-09-08 19:37 CDT (desktop session)

Back on the **desktop** checkout (`C:\Users\bvlsc\Desktop\crimsonland-freshclone`,
venv `.pth` points here). Pulled in the laptop's `6c964517b` / `6a3c3ac0f` /
`870dce9eb` at the start, everything below is on top of that.

## Repo: one branch, renamed to `main`

- `feat/roguelite-weapons` **deleted** from the remote.
- `master` renamed to **`main`** (pushed `main`, changed the GitHub default,
  deleted `master`). There is now exactly one branch. No PR flow — commit and
  `git push` straight to `main`.
- **Laptop must migrate its local branch** on next pull:
  ```
  git branch -m master main   # or feat/roguelite-weapons main
  git fetch origin
  git branch -u origin/main main
  git remote set-head origin -a
  ```
  or just re-clone. `SETUP.md` already says `main`.
- `SETUP.md` at the repo root is the canonical "stand up on another machine"
  doc (install via uv, run, tests, mod-content table). §6 mod table is current.

## Arc Gun sound (commits `fdb000384` .. `7773272b6`)

- Optional best-effort SFX channel added (`grim/sfx_map.py` `SFX_OPTIONAL_SPECS`,
  `grim/sfx.py` loader + `sample_optional()` + graceful `play_sfx`). Loads a PAQ
  entry if present, else the committed fallback in `src/grim/optional_sfx/`.
- `SfxId.ARC_LIGHTNING` = `lightning_sound.ogg` (committed fallback in the repo,
  and injected into the local `runtime/sfx.paq`).
- The Arc Gun's per-shot `SHOCK_FIRE_ALT` fire cue is **suppressed**
  (`plan_player_audio_sfx` / `AudioRouter` special-case `WeaponId.RAYGUN`). Its
  only voice is the crackle.
- The crackle plays **once per shot** on the soft-sfx channel (volume 0.3), not
  on a timed loop — the 26-round clip empties in ~2s so a 1.6s loop only fired
  once or twice. Voice polyphony overlaps the tails into a continuous stream
  while firing; stops instantly on reload. Removed `ARC_SOUND_LOOP_S` /
  `arc_sound_loop_index` / `ArcGunState.sound_elapsed`.

## Evil Scythe (commits `ccda668d1` .. `e7db4583a`, `1257383c2`)

- Silent swing — per-shot fire cue suppressed the same way as the Arc Gun.
- Debug/green overlay in `render/world/scythe.py` **removed** (was the green
  reach ring / cone edges / per-creature circles). Blade sprite trail opacity
  raised (leading copy solid).
- **Heavier / slower, same DPS:** swing damage 45.6 -> **91.2**, `shot_cooldown`
  0.3 -> **0.6**, `reload_time` 0.5 -> **1.0**, `SCYTHE_SWING_DURATION_S` 0.28 ->
  **0.56**. `(2*91.2)/(2*0.6+1.0)` == `(2*45.6)/(2*0.3+0.5)` ~= 83 dps.

## Blade bonus — retuned (commits `9e75a7752` .. `ba7e40be9`)

Iterated live with the user, current state in `bonuses/blade_orbit.py`:

| const | value | was |
|---|---|---|
| `BLADE_RADIUS` (orbit) | **50.0** | 130.0 |
| `BLADE_HIT_RADIUS` | **15.0** | 20.0 |
| `BLADE_REVOLUTIONS` | **6.0** (over the same 5s) | 2.0 |
| `BLADE_HIT_COOLDOWN_S` | **1.5 x `_BLADE_PASS_INTERVAL_S`** (~0.25s) | 0.2 flat |
| `BLADE_HIT_DAMAGE` | **20.0** | 22.0 |

- `_BLADE_PASS_INTERVAL_S = BLADE_DURATION_S / (BLADE_REVOLUTIONS * BLADE_COUNT)`
  — the cooldown is derived from it so it tracks the orbit speed.
- **Intent:** a *stationary* target on the ring is struck by only every **other**
  blade sweeping past it (the blade right behind is still cooling down). Parked
  DPS ~= 76, ~380 per 5s duration, and it's **framerate-independent** (the
  no-cooldown version scaled with fps — a real bug, now fixed).
- Test-mode scaffolding that briefly existed (green debug circles, permanent
  orbit) was **reverted** in `ba7e40be9`. The `--debug`-only blade overlay the
  laptop added in `870dce9eb` (cyan ring / red hitbox / yellow dot) is intact.
  The Blade is a normal timed pickup again; it drops from the regular bonus
  pool, including in `--test-mode`.

## Test mode

`bonuses/update.py`: `_TEST_MODE_BONUS_CYCLE` and `_TEST_MODE_WEAPON_DROPS` are
both empty (unchanged from the laptop). `--test-mode` unlocks the fireable
weapon set; weapons/bonuses arrive via the normal pool.

## Local-only (not in the repo)

- `C:\Users\bvlsc\Desktop\crimson-monster-sprites\` — the 6 creature sprite
  sheets (`zombie`, `lizard`, `alien`, `spider_sp1`, `spider_sp2`, `trooper`),
  512x512 RGBA, decoded from `crimson.paq` via `crimson extract`. Regenerate any
  time with `crimson extract <runtime-dir> <out-dir>`.

## Test status

Green across `tests/bonuses tests/render tests/sim tests/weapons tests/modes
tests/ui tests/gameplay tests/contracts` (~1000+ passed each sweep). The
documented pre-existing `zig` / `match` / `mod_sdk` / `library` failures and the
`tests/replay/cli/test_list.py` Windows path-display failure are unchanged.

## Open (carried from earlier today)

Mouse lower-left drift, "music/sound screwed up", Splitter-Gun-pickup-no-swap —
all still need the user's confirmation / more detail (see the follow-up section
above).
